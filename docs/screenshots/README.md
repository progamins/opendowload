# Capturas — OpenMedia Downloader

Coloca aquí capturas reales (PNG 1600x900 aprox). Nombres esperados por el README y por progamins.github.io:

- `home.png` — Home / input 1-2 enlaces
- `analyzing.png` — Tarjetas en estado ANALYZING / READY
- `downloading.png` — Progreso con bytes/speed/ETA, 2 simultáneas
- `produccion.png` — Terminal `INICIAR.bat → [2] Producción` con tunnel OK y Vercel poll

> Tip: en Windows usa `Win+Shift+S` o la herramienta Recortes. Guarda en esta carpeta y haz `git add docs/screenshots/*.png`.
